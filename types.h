#ifndef TYPES_H
#define TYPES_H

typedef unsigned char byte;       // 8 bit
typedef unsigned short int word;  // 16 bit
typedef word address;              // 16 bit

extern word reg[8];

#endif